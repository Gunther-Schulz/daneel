# DANEEL implement

Render pending. Source: `diligence-framework/spec/core.md` §4.2.

DANEEL's implement phase is thinned compared to Clippy's: simple fixes
(1-3 components, logic corrections, state fixes, parameter
adjustments — no architectural changes) complete inline. Complex fixes
(multi-component architectural, new patterns, pattern-repetition audit
needed) hand off to Clippy's implement phase via explicit cross-instance
handoff.

Render to be authored in Phase 2 of scaffold.
