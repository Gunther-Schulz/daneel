# DANEEL debugging disciplines

Render pending. Source: `legacy/DANEEL.md` D-sections, adapted to render
as DANEEL-specific elaboration of the framework's investigate-design
phase.

Will carry:

- **D2 Purpose Understanding** — clarify correctness criteria before
  investigating wrong behavior
- **D3 Ground Truth Establishment** — source hierarchy, verification map
  format, fact retention + hypothesis tracking
- **D4 Domain Modeling** — document expected behavior, logical
  constraints, semantic validity
- **D5 Concrete Example Tracing** — follow ONE specific case to find
  divergence point
- **D6 Root Cause Analysis** — classify, evaluate fix options, impact
  trace, pattern repetition audit

These render as DANEEL's domain-specific elaboration of the framework's
investigate-design phase content.

Render to be authored in Phase 2 of scaffold.
